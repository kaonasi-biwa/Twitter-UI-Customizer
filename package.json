{
    "devDependencies": {
        "@typescript-eslint/parser": "^6.0.0",
        "eslint-config-prettier": "^8.8.0",
        "prettier": "^3.0.0",
        "web-ext": "^7.6.2",
        "@microsoft/eslint-formatter-sarif": "^2.1.7",
        "eslint": "^8.10.0",
        "node-fetch": "^3.3.2",
        "vite": "^4.4.9"
    },
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "yarn build:copy_assets & yarn build:gen_manifest & yarn build:content & yarn build:other",
        "build:other": "vite build --mode html --watch",
        "build:content": "vite build --mode content",
        "build:copy_assets": "node ./scripts/copy-assets2dist.mjs",
        "build:gen_manifest": "node ./scripts/manifestChange.mjs firefox",
        "preview": "vite preview",
        "debug": "node scripts/run.js",
        "debug firefox": "node scripts/run.js firefox",
        "debug chrome": "node scripts/run.js chrome"
    }
}
